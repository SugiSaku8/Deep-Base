import core_eval from './core_eval.js';
import core_virtual from './core_virtual.js';
import core_build from './core_build.js';

const sandbox = {
  core_eval,
  core_virtual,
  core_build,
};

export default sandbox;